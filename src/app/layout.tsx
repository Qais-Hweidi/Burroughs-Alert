import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Burroughs Alert - NYC Apartment Hunting Made Easy',
  description: 'Get instant notifications for NYC apartments that match your criteria. Find your perfect apartment in Manhattan, Brooklyn, Queens, Bronx, or Staten Island.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className} suppressHydrationWarning>
        {children}
      </body>
    </html>
  )
}